# Workspace Rules

## Automatic GitHub Synchronization
- Automatically commit and push all file changes to GitHub (`git add -A`, `git commit`, `git push origin main`) at the end of handling every user prompt or input.
